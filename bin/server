#!/bin/bash
set -e

# NOTE: This script is intended for use in Docker.

bin/migrate

echo '------------------------------------------------------'
echo 'bundle exec rails server --binding=0.0.0.0 --port=3000'
bundle exec rails server --binding=0.0.0.0 --port=3000
